# Learning Q&A Assistant 项目结构说明

> 本文档帮助你理解 LangchainDemo 项目的目录划分、分层架构设计，以及为什么工业级 RAG 项目不能像初学者示例那样只写一个 Python 文件。

---

## 1. 项目定位

这是一个**多用户 RAG Agent 服务**，实现了三个演进版本：

| 版本 | 特点 |
|---|---|
| V1 | 固定检索 RAG：上传文档 → 切片 → Embedding → 存入 Milvus → 问答时检索 + 生成 |
| V2 | Agent 架构：支持 `knowledge`（仅知识库）/ `web`（Tavily 联网搜索）/ `auto`（自动选择）三种模式 |
| V3 | 规划中：Vue 3 + TypeScript 前端界面 |

技术栈：**Python 3.12 + FastAPI + LangChain + Milvus + PostgreSQL + 千问 Embedding + DeepSeek/Ollama**

---

## 2. 顶层目录一览

```
LangchainDemo/
├── .run/                     # 运行日志（后端/前端的 stdout 和 stderr）
├── .venv/                    # Python 虚拟环境（通过 uv sync 自动生成）
├── app/                      # ⭐ 核心应用代码（分层架构）
├── scripts/                  # 独立工具脚本
│   └── evaluate_retrieval.py #   检索召回率评估脚本
├── scau/                     # 早期实验脚本（仅学习参考，不参与服务）
│   └── agentdemo.py
├── tests/                    # 测试代码
│   ├── fixtures/             #   测试数据
│   └── integration/          #   集成测试
├── .env                      # 环境变量（密钥，不要提交到 Git）
├── .env.example              # 环境变量模板
├── pyproject.toml             # 项目依赖声明
├── uv.lock                   # 依赖锁定文件（确保可复现构建）
├── .gitignore
├── README.md                 # 项目说明（安装、配置、API）
└── 2026-09-09-rag-backend-design.md  # V1 架构设计文档
```

---

## 3. 核心目录 `app/` 分层详解

`app/` 采用**模块化单体 + 五层架构**，从外到内依次为：

```
┌─────────────────────────────────────────────────────┐
│  api/           ← HTTP 接口层（路由、请求/响应模型）  │
├─────────────────────────────────────────────────────┤
│  application/   ← 应用层（业务编排、用例协调）       │
├─────────────────────────────────────────────────────┤
│  workflows/     ← 工作流层（RAG/Agent 的完整执行流）  │
├─────────────────────────────────────────────────────┤
│  domain/        ← 领域层（数据模型 + 抽象接口/协议）  │
├─────────────────────────────────────────────────────┤
│  infrastructure/← 基础设施层（外部系统具体实现）      │
└─────────────────────────────────────────────────────┘
         ↑  所有层共用  ↓
          core/  （配置、异常、日志）
```

### 3.1 `app/api/` — 接口层

| 文件 | 职责 |
|---|---|
| `routes.py` | V1/V2 所有 HTTP 路由定义（文档上传、会话管理、问答） |
| `schemas.py` | Pydantic 请求/响应模型，定义 API 数据格式 |
| `dependencies.py` | FastAPI 依赖注入函数（如从请求头提取 user_id） |
| `middleware.py` | 中间件（如 request_id 注入、错误捕获） |

**作用**：只关心 HTTP 协议细节——接收请求、校验格式、调用应用层、返回响应。不包含业务逻辑。

### 3.2 `app/application/` — 应用层

| 文件 | 职责 |
|---|---|
| `container.py` | 服务装配：把所有依赖实例组合起来，提供 `get_app()` 和就绪检查 |
| `ingestion.py` | 文档入库用例：解析 → 切片 → Embedding → 存入 Milvus + PostgreSQL |
| `chat.py` | 问答用例：V1/V2 的完整问答编排（存历史、调用工作流、保存回答） |
| `sessions.py` | 会话管理 + 单进程并发锁 |
| `evidence.py` | V2 证据编号、去重、引用校验、token 预算计算 |
| `retriever.py` | V1/V2 共用的知识库检索逻辑 |

**作用**：业务编排中枢。它不直接操作 Milvus 或 PostgreSQL，而是调用领域层定义的接口，由基础设施层提供实现。

### 3.3 `app/workflows/` — 工作流层

| 文件 | 职责 |
|---|---|
| `rag.py` | V1 固定 RAG 流程：问题改写 → 检索 → 拼接上下文 → LLM 生成 |
| `agent.py` | V2 Agent 流程：LangChain Agent 按 `search_mode` 在知识库和 Tavily 之间选工具，有限工具循环 |

**作用**：封装端到端的执行流。一个 workflow 就是一条完整的"从输入到输出"的路径。

### 3.4 `app/domain/` — 领域层

| 文件 | 职责 |
|---|---|
| `contracts.py` | 数据类（`Chunk`、`SearchHit`、`ChatTurn`、`ModelAnswer` 等）+ 抽象协议（`EmbeddingProvider`、`VectorStore`、`ChatModelProvider`） |

**作用**：
1. 定义整个系统使用的**核心数据结构**（纯 dataclass，不依赖任何外部库）
2. 定义**抽象接口（Protocol）**，描述"向量存储应该有哪些方法""Embedding 模型应该有哪些方法"，但不指定具体实现

这一层是**整个项目的依赖锚点**——任何层都可以依赖它，但它不依赖任何外部包。

### 3.5 `app/infrastructure/` — 基础设施层

这一层包含所有对外部系统的具体适配实现：

| 子目录 | 文件 | 职责 |
|---|---|---|
| `persistence/` | `database.py` / `models.py` / `repository.py` | PostgreSQL 连接、SQLAlchemy ORM 模型、数据访问仓储 |
| `vectorstores/` | `milvus.py` | Milvus 向量存储的具体实现，实现 `VectorStore` 协议 |
| `embeddings/` | `qwen.py` | 千问 Embedding API 适配，实现 `EmbeddingProvider` 协议 |
| `llms/` | `providers.py` | DeepSeek / Ollama 适配，实现 `ChatModelProvider` 协议，含有限降级逻辑 |
| `loaders/` | `local.py` / `mineru.py` | TXT/MD/PDF/DOCX 本地解析 + MinerU 云解析服务 |
| `search/` | `tavily.py` | Tavily 网络搜索适配器 |

### 3.6 `app/core/` — 核心层

| 文件 | 职责 |
|---|---|
| `config.py` | 从 `.env` 加载配置（Pydantic Settings） |
| `errors.py` | 自定义异常类型（带错误码和 HTTP 状态码） |
| `logging.py` | 结构化日志配置 |

**作用**：横切关注点，所有层都可以使用。

### 3.7 `app/` 根目录入口

| 文件 | 职责 |
|---|---|
| `main.py` | FastAPI 应用创建入口，注册路由和中间件 |
| `bootstrap.py` | 初始化脚本：创建 PostgreSQL 表、Milvus 集合、V2 迁移 |

---

## 4. 数据流向示例

以 **V2 Agent 问答**为例，请求的完整链路：

```
HTTP 请求
  │
  ▼
api/routes.py         ← 接收 POST /api/v2/sessions/{id}/messages
  │                   ← 用 schemas 校验请求格式
  ▼
application/chat.py   ← ChatService.answer_v2()
  │                   ← 存用户问题到数据库（先落库，保证不丢失）
  │                   ← 读取会话历史
  │                   ← 创建 RunContext
  ▼
workflows/agent.py    ← agent_workflow.run()
  │                   ← LangChain Agent 判断用 knowledge / web / 两者
  │                   ← 调用 retriever（如果选 knowledge）
  │                   ← 调用 tavily（如果选 web）
  ▼
domain/contracts.py   ← 所有中间数据都是 Chunk / SearchHit / ModelAnswer
  ▲
  │ 实现接口
  │
infrastructure/       ← milvus.py / tavily.py / qwen.py / providers.py
  │                   ← 真正与外部服务通信
  ▼
application/chat.py   ← 拿到 result 后，校验引用文档仍存在
  │                   ← 存 assistant 回答到数据库
  ▼
api/routes.py         ← 返回 HTTP 响应
```

---

## 5. 依赖方向（关键规则）

```
api ──→ application ──→ domain
  │                    ↑
  │                    │
  └──→ infrastructure ─┘
              ↑
              │
      core（被所有层依赖）
```

**核心规则：**
- 上层依赖下层（`api` 依赖 `application`，`application` 依赖 `domain`）
- `infrastructure` 实现 `domain` 定义的协议（谁实现谁具体）
- `domain` 不依赖任何其他层 → 可以独立测试、可以被替换实现

---

## 6. 为什么不能只用一个 Python 文件？

初学者示例只需要：加载 PDF → 切片 → Embedding → 存本地 → 构建一条 LangChain chain → 调用。这可以在一个文件里搞定。

但生产环境需要额外处理：

| 需求 | 单文件能做吗 | 分层后怎么做 |
|---|---|---|
| 多用户隔离（每个人有自己的文档空间） | 硬写 user_id 过滤 | domain 协议天然支持，Milvus 实现里加 user_id 过滤 |
| 文档上传是异步的（解析慢，不能阻塞 HTTP） | 难写 | application/ingestion.py 编排后台任务 |
| 文档解析有失败重试、有状态机（processing → ready / failed → retry） | 逻辑混乱 | application 层独立管理 |
| 同时支持 DeepSeek 和 Ollama，失败时降级 | if-else 塞满 | domain 定义接口，infrastructure 各自实现 |
| 测试时不想真调 Milvus，想用内存假的 | 难 mock | domain 协议 → 测试时写一个 FakeVectorStore |
| 替换 Embedding 模型（千问换 BGE-M3） | 改很多地方 | 只写一个新的 `infrastructure/embeddings/bge.py` |
| 文档来源多样化（本地解析 + MinerU 云服务） | if-else | 两个 loader 实现同一套归一化接口 |
| 并发控制、请求 ID、结构化日志、错误码 | 到处散 | core/ + middleware 统一处理 |

**一句话总结**：单文件适合演示"原理通了"，分层适合维护"可运行的产品"。
